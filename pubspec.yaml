name: meu_app_flutter
description: "A new Flutter project."

# Impede que o projeto seja publicado no pub.dev
publish_to: 'none'

# Versão do app
version: 1.0.0+1

# Versão do SDK do Dart
environment:
  sdk: ^3.10.7

# ========================
# 📦 DEPENDÊNCIAS
# ========================
dependencies:


  flutter:
    sdk: flutter
    

  # Ícones padrão estilo iOS
  cupertino_icons: ^1.0.8

  # Suporte a SVG (para usar ícones SVG)
  flutter_svg: ^2.2.3


# ========================
# 🧪 DEPENDÊNCIAS DE DEV
# ========================
dev_dependencies:
  flutter_test:
    sdk: flutter

  # Regras de boas práticas de código
  flutter_lints: ^6.0.0

# ========================
# 🎨 CONFIGURAÇÕES FLUTTER
# ========================
flutter:

  # Garante que os ícones Material estejam disponíveis
  uses-material-design: true

  # ========================
  # 🖼️ ASSETS (ícones, imagens, etc.)
  # ========================
  assets:
    # Pasta onde ficam seus ícones SVG
    - assets/icones/
    - assets/imagens/

  # ========================
  # 🔤 FONTES
  # ========================
  fonts:
    - family: Poppins
      fonts:
        # Fonte base (simples, sem variações por enquanto)
        - asset: assets/fontes/Poppins-Regular.ttf